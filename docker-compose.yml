version: '3'
services:
  postgres:
    container_name: timemanagerdata
    image: postgres
    environment:
      POSTGRES_DB: timemanager
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - 5432:5432

volumes:
  postgres_data: